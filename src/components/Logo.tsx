export function Logo() {
  return (
    <span className="inline-flex items-center text-sm font-semibold tracking-[0.2em] text-white">
      C
      <svg
        viewBox="0 0 8 8"
        className="inline-block w-[0.5em] h-[0.5em] mx-[0.13em] mb-[0.04em] flex-shrink-0"
        aria-hidden="true"
      >
        <circle cx="4" cy="4" r="4" fill="#6366f1" />
      </svg>
      VEIL
    </span>
  );
}
